name: Checking version number
on:
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        - fetch-depth: '0'
    - name: print git diff
      run: |
        git fetch origin main
        git diff main
